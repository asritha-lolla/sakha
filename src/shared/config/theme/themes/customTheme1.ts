import { createTheme } from "@mui/material";

export const customTheme1 = createTheme({
    palette: {
      mode: 'light',
      primary: {
        main: '#4caf50',
      },
      secondary: {
        main: '#ff9800',
      },
    },
  });